# Helper to extract support equations (duplicates logic in run_single_dsep_test)

Helper to extract support equations (duplicates logic in
run_single_dsep_test)

## Usage

``` r
get_dsep_support_equations(test_eq, all_equations, family)
```
