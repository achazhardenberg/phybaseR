
R version 4.5.2 (2025-10-31) -- "[Not] Part in a Rumble"
Copyright (C) 2025 The R Foundation for Statistical Computing
Platform: aarch64-apple-darwin20

R is free software and comes with ABSOLUTELY NO WARRANTY.
You are welcome to redistribute it under certain conditions.
Type 'license()' or 'licence()' for distribution details.

R is a collaborative project with many contributors.
Type 'contributors()' for more information and
'citation()' on how to cite R or R packages in publications.

Type 'demo()' for some demos, 'help()' for on-line help, or
'help.start()' for an HTML browser interface to help.
Type 'q()' to quit R.

> library(phybaseR)
> library(ape)
> 
> # Reproduce MAG bug and print model
> set.seed(123)
> N <- 10 # Small for easier debugging
> tree <- rtree(N)
> 
> df <- data.frame(
+     SP = tree$tip.label,
+     Y1 = rnorm(N, mean = 10, sd = 2),
+     Y2 = rnorm(N, mean = 8, sd = 1.5)
+ )
> 
> cat("Generating MAG model...\n")
Generating MAG model...
> 
> # Just generate the model without running
> model_output <- phybaseR:::phybase_model(
+     eq_list = list(
+         list(response = "Y1", predictors = "L1"),
+         list(response = "Y2", predictors = "L1")
+     ),
+     all_vars = c("Y1", "Y2", "L1"),
+     response_vars = c("Y1", "Y2"),
+     latent = "L1",
+     latent_method = "correlations",
+     structure = tree,
+     structure_names = "phylo",
+     independent = FALSE,
+     optimise = TRUE,
+     multi.tree = FALSE
+ )
Error in phybaseR:::phybase_model(eq_list = list(list(response = "Y1",  : 
  unused arguments (eq_list = list(list(response = "Y1", predictors = "L1"), list(response = "Y2", predictors = "L1")), all_vars = c("Y1", "Y2", "L1"), response_vars = c("Y1", "Y2"), latent_method = "correlations", structure = tree, independent = FALSE)
Execution halted
